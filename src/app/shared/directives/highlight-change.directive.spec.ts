import { HighlightChangeDirective } from './highlight-change.directive';

describe('HighlightChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightChangeDirective();
    expect(directive).toBeTruthy();
  });
});
